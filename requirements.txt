PyMuPDF                 # for reading and extracting text from PDF files
tqdm                    # for displaying progress bars
sentence-transformers   # for creating text embeddings (all-mpnet-base-v2, etc.)
accelerate              # for efficient model loading / device placement
bitsandbytes            # for 4-bit quantization of LLM weights
flash-attn              # for faster attention (optional; may fail on some GPUs)
transformers            # for loading and running Gemma and other Hugging Face LLMs
spacy                   # for sentence segmentation (sentencizer)
torch                   # PyTorch backend for embeddings and LLM
numpy                   # numerical arrays for embeddings and similarity
